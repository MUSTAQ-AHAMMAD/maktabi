# Maktabi Local Development Environment Variables
#
# These variables are used by docker-compose.yml for local development.
# Copy this file to .env for local development:
#   cp .env.example .env
#
# Note: docker-compose.yml has default values hardcoded, so this file is
# optional but recommended to avoid Docker Compose warnings.

# Backend Configuration
DATABASE_URL="postgresql://maktabi:maktabi123@postgres:5432/maktabi_db"
JWT_SECRET="maktabi-super-secret-jwt-key-2024"
FRONTEND_URL="http://localhost:3000"

# Frontend Configuration
NEXT_PUBLIC_API_URL="http://localhost:3001"
